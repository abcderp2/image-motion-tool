# 部分モーションの設計と保守

## 目的

1枚のPNG、JPEG、WebPだけを入力し、利用者が画像上を塗った範囲だけへ既存の動きを適用します。画像の事前分割、外部API、外部ライブラリ、端末外への画像送信は行いません。

## 合成方式

初期方式は元画像を残す重ね描画です。

1. 背景を描く
2. 元画像を動かさずに描く
3. 元画像全体を既存の動きで別Canvasへ描く
4. 動いた画像へ選択マスクをdestination-inで適用する
5. 選択部分だけを元画像の上へ重ねる

動いた部分の元位置を削除しないため、未知の背景を生成する必要がありません。移動量が大きい場合は二重像が見えるため、UIで小さい値から始めるよう案内します。

## 選択マスク

選択マスクは白を選択、透明を未選択として扱うCanvasです。元画像と同じ縦横比を保ちますが、巨大画像と同じ寸法にはしません。

上限は次のとおりです。

- 通常端末は長辺1024px
- deviceMemoryが2GB以下の端末は長辺640px
- 合計786432画素

ブラシの点はマスク座標で保持します。40ストロークへ達した場合は現在の結果を基底マスクへ統合し、それ以降の操作を新しい履歴として保持します。これにより、履歴配列が際限なく増えることを避けます。

範囲を全部消す操作では直前のマスクをImageDataとして1回だけ保持し、「範囲を1つ戻す」で復元できます。

## 座標変換

指やマウスの座標は次の順序で変換します。

1. viewport座標からCanvasのCSS表示座標を求める
2. Canvas内部ピクセル座標へ変換する
3. 画面比率、画像のfit、画像サイズ、位置調整を反映した静止画像領域を求める
4. 左右反転を考慮して0から1の元画像座標へ戻す
5. マスクCanvas座標へ変換する

座標計算はmotion-model.jsのstaticGeometry、sourcePointAt、frameGeometryへ集約しています。画面操作側で別の計算式を増やさないでください。

## スマートフォン操作

Canvasは既存どおりtouch-actionを無効にし、Pointer Eventsを使います。範囲編集中は画像ドラッグを停止し、主ポインターだけで塗ります。編集を終えると通常の画像移動へ戻ります。

操作ボタンは既存の44px以上のタップ領域とbutton-rowを再利用します。新しい折りたたみ領域は作らず、対象、ブラシ、境界、編集、戻す、やり直す、全消去を同じ欄へ表示します。

## 保存とプライバシー

選択マスクは次の保存しません。

- localStorage
- 設定JSON
- Cache Storage
- Service Worker
- 外部サーバー

画像の変更、画像を外す操作、再読み込み、pagehideで破棄します。将来マスク保存を追加する場合は、容量上限、画像由来情報であることの表示、明示的な保存操作、読み込み検査を同じ変更で実装してください。

## 別タブプレビュー

既存の軽量Canvas別タブプレビューは画像URLと通常設定だけを渡します。選択マスクをHTML属性やURLへ埋め込まない方針のため、部分モーション時は無効にします。

部分モーションの確認は、画面内プレビュー、または保存後のGIF、APNG、アニメーションWebP用別タブリンクを使います。別タブプレビューへ対応を追加する場合は、マスクをBlob URLで渡し、CSP、URL破棄、容量上限、preview-page.jsの入力検査を同時に追加してください。

## 性能確認

最低限、次を確認します。

- 320px幅のスマートフォン表示でボタンが横にはみ出さない
- 360px、3秒、10fpsでGIF、APNG、アニメーションWebPが生成できる
- 2GB端末相当の設定でマスク長辺が640px以下になる
- 1440px静止画像でメモリ確保に失敗した場合も画面が停止しない
- 画像サイズ、位置、左右反転を変更しても選択範囲が同じ画像部分へ追従する
- 指を画像外へ動かし、戻した場合に直線が画像外を横断しない
- 編集中は画像ドラッグが動かず、編集終了後は動く
- 範囲の戻す、やり直す、全消去後の復元が動く
- 画像変更後に前のマスクが残らない
- 再読み込み後にマスクが復元されない

## 自動検査

motion-model.jsでは次をテストします。

- 静止画像のfit、中心、表示寸法
- Canvas座標から元画像座標への変換
- 左右反転時の座標反転
- 動き適用後の中心、寸法、支点
- 通常端末と低メモリ端末のマスク寸法上限

partial-motion-mask.js、partial-motion-render.js、partial-motion-app.js、app-events.jsはNodeの構文検査、静的検査、危険なDOM APIの禁止検査を通します。ブラウザ固有のCanvas合成は公開前に実機または主要ブラウザの開発者ツールで確認します。

## 変更時の境界

次の変更は別のPull Requestへ分けます。

- 元位置を消して背景を補完する機能
- 顔、髪、人物の自動認識
- WebGL、WebGPU、機械学習モデルの導入
- 複数マスクへ別々の動きを設定する機能
- マスクの永続保存
- 別タブ軽量プレビューへのマスク転送

初期機能の不具合修正と混ぜると、切り戻し範囲とメモリ上限の判断が曖昧になります。

## 切り戻し

問題が部分モーションだけに限定される場合は、対象Pull Requestをrevertします。既存の画像全体モーション、エンコーダー、設定形式へ変更を広げない構成なので、revert後も従来機能を維持できます。

緊急停止が必要でrevertを待てない場合は、partial-motion-mask.js、partial-motion-render.js、partial-motion-app.js、app-events.jsの対象選択を画像全体へ固定し、部分モーションUIを無効化する最小修正を作成します。その場合もmainへ直接変更せず、専用ブランチとPull Requestを使用します。
